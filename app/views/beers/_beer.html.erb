<div id="<%= dom_id beer %>">

  <p>
    <strong>Style:</strong>
    <%= link_to beer.style %>
  </p>

  <p>
    <strong>Brewery:</strong>
    <%= link_to beer.brewery %>
  </p>

  <p>
    <% if beer.ratings.empty? %>
      Beer has not yet been rated!
    <% else %>
      Beer has <%= pluralize(beer.ratings.count, 'rating') %> with an average of <%= round(beer.average) %>.
    <% end %>
  </p>

</div>
